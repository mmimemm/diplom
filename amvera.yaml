meta:
  name: it-mania
  environment: node
  toolchain:
    name: npm
    version: "18"
build:
  skip: false
  commands:
    - npm install
run:
  command: node server/server.js
  ports:
    - port: 3000
      protocol: http
      target-port: 3000
